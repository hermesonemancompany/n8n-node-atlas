import json, os, re, html
from pathlib import Path
ROOT=Path('/opt/data/n8n-docs-site')
PKG=ROOT/'vendor/package'
OUT=ROOT/'site'
OUT.mkdir(exist_ok=True)
pkg=json.loads((PKG/'package.json').read_text())
entries=pkg['n8n']['nodes']
# Derive inventory from official package node entry paths.
items=[]
for path in entries:
    path=path.replace('dist/','').replace('.node.js','').replace('.js','')
    parts=path.split('/')
    slug=parts[-1]
    display=re.sub(r'(?<!^)(?=[A-Z])',' ',slug).replace(' Node','').strip()
    display=display.title() if display.islower() else display
    low=path.lower()
    if any(x in low for x in ['trigger','webhook','schedule','cron','poll']): cat='Triggers & Webhooks'
    elif any(x in low for x in ['google','microsoft','slack','discord','telegram','email','smtp','notion','airtable','hubspot','salesforce','jira','linear','github','gitlab','twilio']): cat='Apps & SaaS'
    elif any(x in low for x in ['postgres','mysql','mongo','redis','elastic','supabase','sqlite','mariadb','snowflake','mssql','oracle','couchbase']): cat='Databases & Storage'
    elif any(x in low for x in ['openai','anthropic','gemini','ollama','hugging','pinecone','vector','embeddings','langchain','ai']): cat='AI & LangChain'
    elif any(x in low for x in ['http','graphql','ftp','ssh','websocket','rss','xml','html']): cat='HTTP & Data Access'
    elif any(x in low for x in ['file','spreadsheet','csv','json','xml','pdf','compression','binary']): cat='Files & Formats'
    elif any(x in low for x in ['if','switch','merge','split','loop','wait','execute','code','set','edit','filter','sort','limit','aggregate','item']): cat='Flow & Data Transformation'
    else: cat='Utilities & Other'
    items.append({'id':'n8n-nodes-base.'+slug,'slug':slug,'name':display,'category':cat,'source':path})
# unique
seen=set(); items=[x for x in items if not (x['id'] in seen or seen.add(x['id']))]
items.sort(key=lambda x:(x['category'],x['name'].lower()))
json_data=json.dumps(items, ensure_ascii=False).replace('</','<\\/')

def esc(s): return html.escape(str(s), quote=True)

css=r'''
:root{--bg:#07111f;--panel:#0c1a2b;--panel2:#10243a;--line:#1b3854;--text:#e9f2fb;--muted:#8fa8bf;--cyan:#47d7ff;--green:#67e8a5;--amber:#ffcc66;--pink:#f38bba;--shadow:0 18px 60px #0006}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:radial-gradient(circle at 80% -10%,#124064 0,#07111f 38%),var(--bg);color:var(--text);font:15px/1.65 Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,sans-serif}a{color:var(--cyan);text-decoration:none}a:hover{text-decoration:underline}code,pre{font-family:ui-monospace,SFMono-Regular,Menlo,monospace}code{color:#b9f3ff;background:#0a2134;padding:.12em .35em;border-radius:5px}.layout{display:grid;grid-template-columns:280px 1fr;min-height:100vh}.sidebar{position:sticky;top:0;height:100vh;overflow:auto;padding:24px 18px;background:#06101ddd;border-right:1px solid var(--line);backdrop-filter:blur(16px)}.brand{display:flex;gap:11px;align-items:center;font-weight:800;font-size:18px;margin-bottom:25px}.mark{width:34px;height:34px;border-radius:10px;background:linear-gradient(135deg,var(--cyan),#4874ff);display:grid;place-items:center;color:#03111e;font-weight:900}.side-label{font-size:11px;color:var(--muted);text-transform:uppercase;letter-spacing:.14em;margin:22px 9px 7px}.sidebar a{display:block;padding:7px 10px;border-radius:8px;color:#bcd0e2}.sidebar a:hover{background:#12314a;text-decoration:none;color:white}.main{max-width:1380px;width:100%;margin:auto;padding:32px 5vw 100px}.hero{display:grid;grid-template-columns:1.4fr .8fr;gap:28px;align-items:center;padding:42px 0 32px}.eyebrow{color:var(--green);font-weight:800;letter-spacing:.16em;text-transform:uppercase;font-size:12px}.hero h1{font-size:clamp(38px,5vw,68px);line-height:1.02;margin:12px 0 18px;letter-spacing:-.055em}.hero p{font-size:18px;color:#b5c9da;max-width:760px}.hero-art{border:1px solid var(--line);background:linear-gradient(145deg,#0c253d,#091726);border-radius:20px;padding:18px;box-shadow:var(--shadow)}.hero-art svg{width:100%;height:auto}.badges{display:flex;flex-wrap:wrap;gap:8px;margin:20px 0}.badge{padding:6px 10px;border:1px solid #245274;border-radius:999px;color:#bcecff;background:#0b2235;font-size:12px}.section{margin-top:55px;scroll-margin-top:25px}.section h2{font-size:30px;letter-spacing:-.03em;margin:0 0 10px}.section h3{font-size:20px;margin:24px 0 8px}.lead{color:var(--muted);max-width:850px}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px}.card{border:1px solid var(--line);background:linear-gradient(145deg,#0c1e31,#0b1727);border-radius:14px;padding:18px;box-shadow:0 8px 28px #0002}.card h3{margin:0 0 5px;font-size:17px}.card p{margin:6px 0;color:#a9bed1}.tag{display:inline-block;color:var(--green);font-size:11px;text-transform:uppercase;letter-spacing:.08em}.callout{border-left:3px solid var(--cyan);background:#0a2235;padding:15px 18px;border-radius:0 10px 10px 0;margin:18px 0}.callout.warn{border-color:var(--amber);background:#2a2414}.codebox{position:relative;background:#06101b;border:1px solid #1d3b55;border-radius:12px;padding:18px;overflow:auto;color:#cce8f5}.codebox pre{margin:0;white-space:pre-wrap}.twocol{display:grid;grid-template-columns:1fr 1fr;gap:18px}.diagram{background:#071522;border:1px solid var(--line);border-radius:15px;padding:14px;overflow:auto}.diagram svg{min-width:650px;width:100%;height:auto}.catalog-tools{display:flex;gap:10px;flex-wrap:wrap;margin:20px 0}.catalog-tools input,.catalog-tools select{background:#091a2b;border:1px solid #28506e;color:white;border-radius:9px;padding:11px 13px;font:inherit}.catalog-tools input{flex:1;min-width:240px}.node-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.node{border:1px solid #193b57;background:#0a1a2b;padding:13px;border-radius:10px;min-height:108px}.node strong{display:block;color:#e9f5ff;margin-bottom:4px}.node small{color:var(--muted);word-break:break-word}.node.hidden{display:none}.stats{display:flex;gap:24px;flex-wrap:wrap;margin:18px 0}.stat b{font-size:28px;color:var(--cyan);display:block}.stat span{color:var(--muted);font-size:12px}.table-wrap{overflow:auto}.table{width:100%;border-collapse:collapse}.table th,.table td{border-bottom:1px solid var(--line);padding:11px;text-align:left;vertical-align:top}.table th{color:var(--cyan)}footer{margin-top:70px;border-top:1px solid var(--line);padding-top:20px;color:var(--muted)}@media(max-width:1050px){.layout{grid-template-columns:1fr}.sidebar{position:relative;height:auto;border-right:0;border-bottom:1px solid var(--line)}.sidebar nav{display:flex;flex-wrap:wrap;gap:4px}.side-label{width:100%;margin-top:10px}.hero,.twocol{grid-template-columns:1fr}.grid{grid-template-columns:repeat(2,1fr)}.node-grid{grid-template-columns:repeat(3,1fr)}}@media(max-width:620px){.main{padding:22px 18px 70px}.grid,.node-grid{grid-template-columns:1fr}.hero h1{font-size:42px}}
'''

def svg_arch():
 return '''<svg viewBox="0 0 720 360" role="img" aria-label="n8n workflow architecture"><defs><linearGradient id="g" x1="0" x2="1"><stop stop-color="#47d7ff"/><stop offset="1" stop-color="#67e8a5"/></linearGradient><marker id="a" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0 L0,6 L7,3 z" fill="#70bfdc"/></marker></defs><rect x="8" y="8" width="704" height="344" rx="18" fill="#081a2a" stroke="#234b68"/><text x="30" y="39" fill="#8fa8bf" font-size="13">EVENT-DRIVEN AUTOMATION</text><g font-family="system-ui" font-size="15" text-anchor="middle"><rect x="34" y="78" width="130" height="62" rx="10" fill="#12314a" stroke="#47d7ff"/><text x="99" y="105" fill="white">Trigger</text><text x="99" y="126" fill="#8fa8bf" font-size="11">Webhook / Schedule</text><rect x="213" y="78" width="130" height="62" rx="10" fill="#12314a" stroke="#67e8a5"/><text x="278" y="105" fill="white">Normalize</text><text x="278" y="126" fill="#8fa8bf" font-size="11">Edit Fields / Code</text><rect x="392" y="78" width="130" height="62" rx="10" fill="#12314a" stroke="#ffcc66"/><text x="457" y="105" fill="white">Decide</text><text x="457" y="126" fill="#8fa8bf" font-size="11">If / Switch</text><rect x="571" y="78" width="115" height="62" rx="10" fill="#12314a" stroke="#f38bba"/><text x="628" y="105" fill="white">Deliver</text><text x="628" y="126" fill="#8fa8bf" font-size="11">HTTP / SaaS</text><path d="M164 109h48M343 109h48M522 109h49" stroke="#70bfdc" stroke-width="2" marker-end="url(#a)"/><rect x="110" y="218" width="500" height="82" rx="13" fill="#0c2538" stroke="#285b7b"/><text x="360" y="247" fill="#47d7ff">Operational boundary</text><text x="360" y="270" fill="#b5c9da" font-size="12">credentials • retries • error workflow • observability • idempotency</text><path d="M278 140v45M457 140v45M628 140v45M99 140v45" stroke="#41677e" stroke-dasharray="5 5"/></g></svg>'''

html_doc=f'''<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="A detailed, searchable atlas of n8n core nodes, patterns, diagrams, and production examples."><title>n8n Node Atlas — Engineering Documentation</title><style>{css}</style></head><body><div class="layout"><aside class="sidebar"><div class="brand"><span class="mark">n8</span> Node Atlas</div><nav><div class="side-label">Start here</div><a href="#overview">Overview</a><a href="#mental-model">Mental model</a><a href="#architecture">Architecture diagram</a><div class="side-label">Engineering guide</div><a href="#patterns">Production patterns</a><a href="#expressions">Expressions & data</a><a href="#reliability">Reliability & security</a><a href="#recipes">Workflow recipes</a><div class="side-label">Reference</div><a href="#catalog">Core node catalog</a><a href="#coverage">Coverage & provenance</a></nav></aside><main class="main"><section class="hero" id="overview"><div><div class="eyebrow">Engineering reference · generated catalog</div><h1>n8n nodes, explained like a system.</h1><p>A practical documentation site for designing reliable automations: every node in the official <code>n8n-nodes-base</code> package, grouped by responsibility, plus architecture diagrams, data contracts, failure handling, security, and reusable workflow examples.</p><div class="badges"><span class="badge">n8n Core {pkg.get('version')}</span><span class="badge">{len(items)} node entries</span><span class="badge">Static · GitHub Pages ready</span></div></div><div class="hero-art">{svg_arch()}</div></section><section class="section" id="mental-model"><h2>The mental model</h2><p class="lead">n8n is an item-processing runtime. A workflow is a directed graph where triggers create items, nodes transform or act on them, and connections define execution order. Production quality comes from explicit contracts around every side effect.</p><div class="grid"><article class="card"><span class="tag">01 · Input</span><h3>Triggers establish the contract</h3><p>Webhook, Schedule, polling, and app triggers define when a run starts and what the first item looks like. Document authentication, payload shape, replay behavior, and response timing.</p></article><article class="card"><span class="tag">02 · Transform</span><h3>Items are the unit of work</h3><p>Prefer expressions and Edit Fields for simple mappings. Preserve stable identifiers, normalize types at boundaries, and avoid hidden dependence on item position.</p></article><article class="card"><span class="tag">03 · Side effect</span><h3>Actions need safeguards</h3><p>API calls, database writes, messages, and file operations are side effects. Add idempotency keys, retries with limits, timeouts, and an error path before publishing.</p></article></div></section><section class="section" id="architecture"><h2>Reference architecture</h2><p class="lead">A maintainable workflow separates ingress, normalization, decisioning, effectful work, and operations. This same shape scales from a three-node automation to a multi-system integration.</p><div class="diagram">{svg_arch()}</div><div class="twocol"><div class="callout"><b>Design rule:</b> keep credentials in n8n credentials, not expressions or plain text. Treat incoming data and remote responses as untrusted.</div><div class="callout warn"><b>Execution rule:</b> branches are not automatically concurrent. If parallelism matters, design and measure it explicitly with sub-workflows or external workers.</div></div></section><section class="section" id="patterns"><h2>Production patterns</h2><div class="grid"><article class="card"><h3>Idempotent webhook</h3><p>Derive a deterministic event key from provider ID + event type. Look it up before writing, then record the key with the result. Return a stable response for retries.</p></article><article class="card"><h3>Paginated API ingestion</h3><p>Use the API’s cursor or next URL, stop on an explicit termination condition, cap pages, and persist the cursor only after successful processing.</p></article><article class="card"><h3>Dead-letter handling</h3><p>Route failures to an error workflow with execution ID, node, correlation ID, sanitized input summary, and retry classification. Never leak secrets into alerts.</p></article><article class="card"><h3>Fan-out / fan-in</h3><p>Split a collection only when each item can be processed independently. Aggregate by a stable key and define behavior for partial failure.</p></article><article class="card"><h3>Human approval gate</h3><p>Persist the pending decision, send a signed or authenticated approval link, validate state on callback, and make approve/reject idempotent.</p></article><article class="card"><h3>Sub-workflow boundary</h3><p>Extract reusable multi-step logic into a stateless sub-workflow. Define inputs and outputs as a versioned contract; avoid hidden credential or global-state coupling.</p></article></div></section><section class="section" id="expressions"><h2>Expressions & data contracts</h2><div class="twocol"><div><h3>Expression essentials</h3><div class="codebox"><pre>{{ $json.customer.email }}
{{ $json.items.map(item => item.sku) }}
{{ $now.toISO() }}
{{ $('Normalize').item.json.customerId }}</pre></div><p class="lead">Use expressions for local mapping and validation. Use a Code node only when the transformation genuinely needs multi-item aggregation, a library, or stateful logic.</p></div><div><h3>Boundary contract checklist</h3><ul><li>Required fields and types are explicit.</li><li>IDs are stable and copied forward.</li><li>Dates use an agreed timezone and format.</li><li>Null, empty, and missing are distinguished.</li><li>Binary data has a size and retention policy.</li><li>Remote errors are classified as retryable or terminal.</li></ul></div></div></section><section class="section" id="reliability"><h2>Reliability, security & observability</h2><div class="table-wrap"><table class="table"><thead><tr><th>Concern</th><th>Implementation guidance</th><th>Verification</th></tr></thead><tbody><tr><td>Timeouts</td><td>Set bounded timeouts on remote calls; do not let a single provider hold a worker indefinitely.</td><td>Test slow response and network failure.</td></tr><tr><td>Retries</td><td>Retry only transient failures, with a cap and backoff. Never blindly retry non-idempotent writes.</td><td>Replay the same event twice.</td></tr><tr><td>Secrets</td><td>Use n8n credentials and least privilege. Redact tokens, cookies, and authorization headers from logs.</td><td>Inspect exported workflow JSON and alerts.</td></tr><tr><td>Rate limits</td><td>Honor provider limits, batch safely, and persist cursors for resumability.</td><td>Run against a throttled test endpoint.</td></tr><tr><td>Observability</td><td>Carry a correlation ID; capture outcome, duration, provider status, and item counts.</td><td>Trace one event end to end.</td></tr><tr><td>Recovery</td><td>Define error workflow, dead-letter destination, replay procedure, and ownership.</td><td>Perform a documented recovery drill.</td></tr></tbody></table></div></section><section class="section" id="recipes"><h2>Workflow recipes</h2><div class="grid"><article class="card"><span class="tag">Lead intake</span><h3>Webhook → Validate → CRM → Notify</h3><p>Webhook receives a lead; Edit Fields normalizes; If validates required fields; CRM creates/upserts; Slack or email notifies. Add deduplication on external lead ID.</p></article><article class="card"><span class="tag">Data sync</span><h3>Schedule → HTTP pagination → Transform → Database</h3><p>Schedule starts a bounded sync. HTTP Request follows cursors. Split/transform items. Database upserts by natural key. Persist the last successful cursor.</p></article><article class="card"><span class="tag">AI enrichment</span><h3>Trigger → Guardrails → AI → Structured output → Review</h3><p>Validate and minimize input before the model. Require structured output, validate it, record model/version, and route low-confidence results to human review.</p></article><article class="card"><span class="tag">File pipeline</span><h3>Drive trigger → Download → Parse → Archive</h3><p>Check file type and size, keep binary handling explicit, parse into structured items, write results, and move/archive only after downstream success.</p></article></div></section><section class="section" id="catalog"><h2>Core node catalog</h2><p class="lead">Generated from the official <code>n8n-nodes-base</code> package manifest. Search by name, package path, or responsibility. Each card links the canonical source path so the catalog remains honest as n8n evolves.</p><div class="stats"><div class="stat"><b id="total">{len(items)}</b><span>catalog entries</span></div><div class="stat"><b id="visible">{len(items)}</b><span>shown</span></div><div class="stat"><b>{len(set(x['category'] for x in items))}</b><span>categories</span></div></div><div class="catalog-tools"><input id="search" type="search" placeholder="Search nodes, IDs, or source paths…" aria-label="Search node catalog"><select id="category"><option value="">All categories</option>{''.join(f'<option>{esc(c)}</option>' for c in sorted(set(x['category'] for x in items)))}</select></div><div class="node-grid" id="nodes">{''.join(f'<article class="node" data-name="{esc(x["name"]+" "+x["id"]+" "+x["source"])}" data-cat="{esc(x["category"])}"><span class="tag">{esc(x["category"])}</span><strong>{esc(x["name"])}</strong><small><code>{esc(x["id"])}</code><br>{esc(x["source"])}</small></article>' for x in items)}</div></section><section class="section" id="coverage"><h2>Coverage & provenance</h2><div class="callout"><b>What “every node” means here:</b> this site catalogs every node entry declared by the pinned official core package version <code>{pkg.get('version')}</code> at build time. n8n also has community nodes and version-specific parameters; those are not silently represented as core coverage.</div><p class="lead">For exact operation parameters, credentials, and conditional fields, use the live n8n instance’s node metadata. Always validate a node configuration against the target n8n version before publishing a workflow. This documentation provides engineering patterns and orientation; it is not a replacement for n8n’s versioned reference.</p><h3>Build metadata</h3><ul><li>Package: <code>n8n-nodes-base</code></li><li>Package version: <code>{pkg.get('version')}</code></li><li>Catalog entries: <code>{len(items)}</code></li><li>Generated: static HTML; no runtime backend or credentials.</li></ul></section><footer>Built as a static, searchable engineering reference for n8n. No credentials or private workflow data are included. · <a href="https://docs.n8n.io/">Official n8n documentation</a></footer></main></div><script>const q=document.querySelector('#search'),c=document.querySelector('#category'),cards=[...document.querySelectorAll('.node')],v=document.querySelector('#visible');function filter(){{const s=q.value.toLowerCase(),cat=c.value;let n=0;cards.forEach(x=>{{const ok=(!s||x.dataset.name.toLowerCase().includes(s))&&(!cat||x.dataset.cat===cat);x.classList.toggle('hidden',!ok);if(ok)n++}});v.textContent=n}}q.addEventListener('input',filter);c.addEventListener('change',filter);</script></body></html>'''
(OUT/'index.html').write_text(html_doc)
(OUT/'.nojekyll').write_text('')
readme=f'''# n8n Node Atlas\n\nA static engineering documentation site for n8n core nodes.\n\n- Official package: `n8n-nodes-base@{pkg.get('version')}`\n- Catalog entries: {len(items)}\n- Includes architecture diagrams, production patterns, expressions, reliability/security guidance, recipes, and searchable node inventory.\n- Generated output: `site/index.html`\n\n## Scope\n\nThe catalog is generated from the official core package manifest at build time. n8n community nodes and version-specific operation parameters are not represented as core coverage.\n'''
(ROOT/'README.md').write_text(readme)
(ROOT/'package.json').write_text(json.dumps({'name':'n8n-node-atlas','private':True,'scripts':{'build':'python3 build_site.py','check':'python3 -c "from pathlib import Path; p=Path(\'site/index.html\'); assert p.exists() and p.stat().st_size>10000; print(p.stat().st_size)"'}},indent=2))
print({'package_version':pkg.get('version'),'catalog_entries':len(items),'html_bytes':(OUT/'index.html').stat().st_size})
